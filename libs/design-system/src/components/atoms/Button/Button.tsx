import { css } from '@alareis/styled-system/css'

export const Button = () => {
  return (
    <button
      className={css({
        backgroundColor: 'purple',
      })}
    >
      A
    </button>
  )
}
